# Pulseflow Application

Pulseflow is a powerful analysis platform that monitors Git repository metrics     
(like churn and module activity) using a FastAPI backend, a DuckDB storage engine, and a React (Vite + Tailwind CSS) frontend.